# ============================================================================
# Configurações de Ambiente - Governance Gateway (EXEMPLO)
# ============================================================================
# Este é um arquivo de exemplo. Copie para .env e configure os valores reais.
#
# Comando: cp .env.example .env
#
# IMPORTANTE: O arquivo .env contém informações sensíveis e NÃO deve ser
# versionado no Git. Ele está listado no .gitignore.

# ----------------------------------------------------------------------------
# Google Cloud Project ID
# ----------------------------------------------------------------------------
# ID do seu projeto no Google Cloud Platform
# Obtenha executando: gcloud config get-value project
#
# Exemplo: fiap-bv-ia-2025
GOOGLE_CLOUD_PROJECT=seu-project-id-aqui

# ----------------------------------------------------------------------------
# Região do Vertex AI
# ----------------------------------------------------------------------------
# Região onde o Vertex AI processará as requisições
# Escolha a região mais próxima para menor latência
#
# Regiões disponíveis:
# - us-central1 (Iowa, USA)
# - us-east1 (South Carolina, USA)
# - us-west1 (Oregon, USA)
# - europe-west1 (Belgium)
# - europe-west4 (Netherlands)
# - asia-east1 (Taiwan)
# - asia-southeast1 (Singapore)
#
# Padrão: us-east1
GOOGLE_CLOUD_LOCATION=us-east1

# ----------------------------------------------------------------------------
# Toggle: Simulação vs Produção
# ----------------------------------------------------------------------------
# Controla se o sistema usa simulação (mock) ou API real do Vertex AI
#
# true  = Modo simulação (sem autenticação, sem custos, respostas mock)
# false = Modo produção (requer autenticação ADC, gera custos reais)
#
# IMPORTANTE: 
# - Modo simulação: Ideal para desenvolvimento e testes
# - Modo produção: Requer 'gcloud auth application-default login'
#
# Padrão: true (simulação)
USE_MOCK=true

# ----------------------------------------------------------------------------
# Configurações Opcionais (Futuras Aulas)
# ----------------------------------------------------------------------------

# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Timeout para chamadas ao Vertex AI (em segundos)
# VERTEX_AI_TIMEOUT=30

# Máximo de tentativas em caso de erro
# MAX_RETRIES=3
